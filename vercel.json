{
    "outputDirectory": "dist",
    "installCommand": "npm install",
    "buildCommand": "NODE_ENV=production DEBUG=* npm run build",
    "devCommand": "npm run start",
    "routes": [
        { "handle": "filesystem" },
        { "src": "/(.*)", "status": 404, "dest": "/404" }
      ]
}